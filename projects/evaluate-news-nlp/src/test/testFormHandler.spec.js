import { handleSubmit} from "../client/js/formHandler.js";

describe("Testing the handleSubmit method", () => {

    test("Making sure handleSubmit is defined", () => {
        expect(handleSubmit).toBeDefined();
      });

})